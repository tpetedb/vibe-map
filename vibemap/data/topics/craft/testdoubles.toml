# The test pyramid and test doubles. Written from the official docs, nothing from memory.
id = "testdoubles"
title = "The test pyramid and test doubles"
age = "castle"
shelf = "code"
depth = 2
# The day every source below was read. Move it when you check them again.
checked = 2026-09-24
minutes = 20

summary = '''The test pyramid is a picture of how many tests of each kind a codebase should have: many small, fast unit tests at the bottom, fewer coarse-grained tests in the middle, and very few end-to-end tests on top. A test double is whatever stands in for a real thing during a test: a dummy, a fake, a stub, a spy or a mock. Reach for this topic when a test is slow, flaky or needs the network, and you want to replace the slow part without losing what the test proves.'''
for_agents = '''And for an agent: patch a name where the code under test looks it up, not where it is defined, and assert on the calls you care about with assert_called_once_with rather than on how the code is built inside.'''

history = '''Ham Vocke's The Practical Test Pyramid, published on martinfowler.com on 26 February 2018, credits Mike Cohn with the test pyramid in his book Succeeding with Agile, with unit tests, service tests and user interface tests from bottom to top, and sums it up as: "Write lots of small and fast unit tests. Write some more coarse-grained tests and very few high-level tests that test your application from end to end." Martin Fowler's TestDouble note of 17 January 2006 records Gerard Meszaros's term "test double", "a generic term for any case where you replace a production object for testing purposes". It names five kinds: dummies are passed around but never used, fakes have working implementations that take a shortcut, stubs "provide canned answers to calls made during the test", spies are stubs that also record how they were called, and mocks "are pre-programmed with expectations". Python's standard library has carried unittest.mock since version 3.3, which lets you "replace parts of your system under test with mock objects and make assertions about how they have been used". Its docs give the rule that trips most people: "you patch where an object is looked up, which is not necessarily the same place as where it is defined."'''

try_it = '''Write test_notify.py: a function notify(user, send) that calls send(user, "welcome"), and a unittest test that passes a Mock() as send and calls send.assert_called_once_with("ada", "welcome"). End the file with unittest.main() and run python3 test_notify.py. Then change "welcome" in notify and watch the test fail with the call it expected.'''

unlocks = ["fixtures", "refactoring"]
prerequisites = ["tests", "python"]

[[origins]]
place = "python-software-foundation"
year = 2012
what = "Python 3.3.0, released on 29 September 2012 on python.org, the Python Software Foundation's site, lists the new unittest.mock module among its major new features."
source = "https://www.python.org/downloads/release/python-330/"
primary = true

[[sources]]
label = "Ham Vocke on martinfowler.com, The Practical Test Pyramid"
url = "https://martinfowler.com/articles/practical-test-pyramid.html"
checked = 2026-09-24

[[sources]]
label = "Martin Fowler, TestDouble"
url = "https://martinfowler.com/bliki/TestDouble.html"
checked = 2026-09-24

[[sources]]
label = "Python documentation, unittest.mock, mock object library"
url = "https://docs.python.org/3/library/unittest.mock.html"
checked = 2026-09-24

[[sources]]
label = "Source: python.org, Python 3.3.0 release page"
url = "https://www.python.org/downloads/release/python-330/"
checked = 2026-09-24

# The hands-on: a folder in the workspace and one offline check.
[hands_on]
title = "A mock that checks the call"
minutes = 15
done = "python3 test_notify.py exits 0 and the test asserts the call with assert_called_once_with"
check = { kind = "script", file = "test_notify.py", contains = ["Mock(", "assert_called_once_with", "unittest.main("], prints = [] }
