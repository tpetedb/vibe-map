# Rendered by .agents/utils/harness.py sync from config.toml,
# .agents/conf/profiles/balanced.toml and .agents/conf/policy.toml; edit the
# source, then sync.
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[sandbox_workspace_write]
writable_roots = ["../.git/board"]
