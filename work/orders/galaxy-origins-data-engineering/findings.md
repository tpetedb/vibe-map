# What

The same as the four core origin orders, for the sixteen topics of the data-engineering pack. `places.problems()` takes `packs=` as well as `shelves=`; if `galaxy-places-schema` did not build that, say so and stop rather than edit `vibemap/places.py`, which is not yours.
