# Hunt Q3: generated notes own Git and Python

The generated `Git` and `Python` topic notes are overwritten by handwritten
cards of the same title in `src/game/50-notes.js`. Delete those cards so the
topic data remains the source of course content, add the official Kaggle Learn
source to the Python topic, and make `tools/build.py` reject duplicate titles
across generated notes, `50-notes.js`, and `51-notes-dynamic.js`.

Source: https://www.kaggle.com/learn
