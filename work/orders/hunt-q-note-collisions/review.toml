# order: hunt-q-note-collisions
# role: reviewer
# [Claude Opus 5.5, effort high, reviewer]
v = 1
order = "hunt-q-note-collisions"
by = "reviewer-hunt-q-note-collisions-r1"
reviewed = "5057828980029adcdd06254a1904af7a31a53892"
verdict = "improve"

[[criteria]]
id = "c1"
verdict = "pass"
evidence = '''
Ran `uv run pytest -q tests/test_hunt_q.py tests/test_build.py tests/test_topics.py` on 5057828: 67 passed. The command proves the sentence, checked by reverting the fixes in scratch exports of HEAD (/tmp/hq-imrL):
- tools/build.py from origin/main (no guard): all 11 collision spellings and the three-owner diagnostic test fail, "the build accepted two notes with one title".
- src/game/50-notes.js from origin/main (handwritten Git and Python back): test_q3_no_tree_note_is_shadowed fails, and the build itself refuses.
- tools/build.py with the column-zero `^"...":\{t:` regex the earlier review read: `spaced key`, `dynamic spaced brackets` and `test_q3_collision_diagnostic_includes_a_spaced_handwritten_key` fail. This is the previous IMPROVE finding, and it is fixed.
- tools/build.py from the transfer commit 0fac83e (whitespace-tolerant regex): 9 of 11 spellings fail, and the three "a read is not a note" guards fail as false positives (a comparison, a template, a comment). The token lexer is what makes them pass.
In the built game there is exactly one `"Git":{t:` and one `"Python":{t:`. I opened both in Chromium through `window.openNote`: both carry the generated "Try in five minutes" text, Python links Kaggle Learn, and there were zero page errors. On origin/main the same probe shows the handwritten Python card instead.
I also tried to break the lexer with valid JavaScript. It catches `U` escapes, template keys, chained assignments and campaign-derived duplicates, and a real regex or division does not trip it. It misses a computed object key, Object.assign, a `\u{55}` escape and a concatenated key (low finding below). Those are other constructs, not spellings a hand edit produces, so I rule the criterion met.
'''

[[criteria]]
id = "c2"
verdict = "pass"
evidence = '''
The same local run covers tests/test_build.py and tests/test_topics.py: all pass. `tools/build.py --check` takes 0.3 s with the lexer, so the guard adds no noticeable build time. CI's browser shards were still pending when I looked. The unit job is red, but for the changelog fragment (finding 1), not for these files.
'''

[[criteria]]
id = "c3"
verdict = "pass"
evidence = '''
Ran the exact chain (ruff check, ruff format --check, checks.py style, checks.py sinks): exit 0, "style OK: no em dashes, no emoji". I read the whole build.py diff. No value reaches HTML: the lexer only reads the sources, and its output goes into a SystemExit message on the terminal. The Kaggle URL goes through the topic data like every other source.
'''

[[criteria]]
id = "c4"
verdict = "pass"
evidence = '''
Ran the loop over build, regen_tree, gen_cookbook, sync_template, sync_fork_source and gen_syllabus with --check: every one exited 0. `diff tools/build.py vibemap/data/fork_source/tools/build.py` is empty. The regenerated outputs in the diff (game, tools/generated/notes.js, docs/ROADMAP.md, the fork_source mirrors) differ only by the Kaggle source and the two removed cards.
'''

[[criteria]]
id = "c5"
verdict = "pass"
evidence = '''
Each Q3 regression test fails without its fix (see c1). The shadow test fails with the cards restored. Every collision case and the three-owner diagnostic fail on main's build.py. The whitespace cases fail on the regex the earlier review read. The read-is-not-a-note guards fail on the 0fac83e regex.
The diagnostic names every source. Each of the 11 parametrized cases asserts the complete owner list (including a title written twice in one file). The spaced-plus-dynamic case names generated, 50-notes.js and 51-notes-dynamic.js. My own probes printed full owner lists, for example `'Evening 2': src/game/51-notes-dynamic.js, src/game/51-notes-dynamic.js`.
Kaggle Learn: GET https://www.kaggle.com/learn answered 200 with the title "Learn Python, Data Viz, Pandas & More | Tutorials | Kaggle". It is Kaggle's own course page, so the source is official. A HEAD request gets a 404 from Kaggle, which is worth knowing for the weekly link check.
'''

[[findings]]
severity = "medium"
file = "changelog.d/note-title-collisions.fixed.md"
text = '''
The fragment is one bare sentence, not a bullet list. `uv run pytest tests/test_changelog.py` fails locally with "note-title-collisions.fixed.md: an entry is a bullet list, starting with -". CI fails the same way: run 35925112250, job "lint, unit tests, generated files in sync", on 5057828: 1 failed, 733 passed. The branch cannot land red. Start the entry with "- " and wrap it like its neighbours. While editing it, drop "however the JavaScript spells it": the guard reads literal keys and literal NOTES assignments, not every construct (next finding). This is the only change I need. The code and tests above are accepted as they are.
'''

[[findings]]
severity = "low"
file = "tools/build.py"
text = '''
`_note_keys` (line 319) and `_assigned_title` (line 308) still accept four valid ways to define a duplicate, each proven with a scratch build that exited 0 while "Unix and the terminal" was duplicated: a computed object key `["Unix and the terminal"]:{...}` in 50-notes.js, `Object.assign(NOTES,{...})` in 51-notes-dynamic.js, a `"\u{55}nix..."` escape (`_string` at line 251 falls back to the raw text when literal_eval refuses JavaScript-only escapes), and a concatenated key `NOTES["Unix and "+"the terminal"]=`. Nobody writes these by accident here. A lexer test that asserts they are refused, or a comment that names the forms that are read, would keep the claim honest.
'''

[[findings]]
severity = "low"
file = "tools/build.py"
text = '''
`_dynamic_note_keys` (line 347) mirrors in Python how 51-notes-dynamic.js derives note titles from CAMPAIGN and MENTORS: the island title before ": ", each stop's `n`, each mentor's name, skipping campus. If that JavaScript changes (for example it starts writing campus stops, or another data-driven loop is added), the guard stops seeing those titles and does not say so. A comment in 51-notes-dynamic.js pointing at the mirror, or a test that compares the mirror with the titles the page actually creates, would close it.
'''

[[findings]]
severity = "low"
file = "tests/test_hunt_q.py"
text = '''
`test_q3_no_tree_note_is_shadowed_by_a_handwritten_one` (line 76) still finds handwritten titles with the compact regex `^"([^"]+)":\{t:`, the same narrow spelling the earlier review faulted in the build. The build guard now covers the real risk, but this test would miss a spaced or single-quoted Git card. Reading the titles through `build._note_keys` would make it agree with the build.
'''

[[findings]]
severity = "low"
file = "work/orders/hunt-q-note-collisions/order.toml"
text = '''
The order has `cross = ["harness", "content"]`, and tools/work.py refuses acceptance without `signoff-harness.toml` and `signoff-content.toml`. Neither file exists on the branch yet. This is not a defect in the work, but `just work-accept` will stop on it.
'''
