v = 1
order = "board-and-memory"
by = "codex-reviewer-board-192"
reviewed = "f2fe6ee713188cf1b1884e8aaac13d21f15c36c4"
verdict = "improve"

[[criteria]]
id = "c1"
verdict = "pass"
evidence = "I ran focused pytest with UV_CACHE_DIR in /private/tmp and -p no:cacheprovider: 39 tests passed. I read both test files: real processes race 120 room appends and 200 memory additions, check stable ids, whole entries and no lost entities; the 300-entity fixture checks digest length; lint cases cover caps, malformed observations and secret patterns; both hook commands run with missing memory, including older Python; index rebuild idempotence is asserted. The live reader printed the digest and the 11-entity store passed memory-lint."

[[criteria]]
id = "c2"
verdict = "pass"
evidence = "I checked the paths and grepped .codex, .claude, justfile and tools/board.py: scripts/memory-mcp.sh and .mcp.json are absent, with no server-memory, read_graph or memory-serve reference there. Live memory-lint reports 11 valid entities. codex mcp list on codex-cli 0.156.1 lists no memory server. Some seeded facts still mention the retired server; see low finding."

[[criteria]]
id = "c3"
verdict = "pass"
evidence = "I ran python3 tools/board.py read in the linked worktree; it resolved /Users/maxgroupit/vibe-map/.git/board/ROOM.md. After an explicit-root Codex probe appended entry 22bd1d92, the same reader showed it. The passing focused test creates a real main checkout and linked worktree and invokes each checkout's own board.py. The criterion shell command alone is weaker because it runs this checkout's script from another cwd."

[[criteria]]
id = "c4"
verdict = "pass"
evidence = "I parsed work/teams.toml and .codex/config.toml: all 12 named harness paths are present and the only declared extra root is ../.git/board. I read their diff. This proves the stored value, not its linked-worktree behavior, which fails c6."

[[criteria]]
id = "c5"
verdict = "fail"
evidence = "My scoped reruns passed ruff on changed Python, sync_template.py --check and c1 tests. I did not run the full non-browser battery or every generator; just work-review reports a cached builder pass on this commit. My current uv run python tools/work.py validate exits 1: collision: board-and-memory and harness-core both own .codex/config.toml; it reports 38 orders and one collision this checkout answers for. Concurrent stacked harness-core work may explain it but does not make c5 pass."

[[criteria]]
id = "c6"
verdict = "fail"
evidence = "Installed codex-cli 0.156.1: fresh codex exec in this linked worktree with --add-dir /Users/maxgroupit/vibe-map/.git/board started, but every shell tool failed before execution: failed to inspect Seatbelt writable root .../claude-board-and-memory/.git/board: Not a directory (os error 20). Its first turn had no SessionStart digest. A second fresh session with -c sandbox_workspace_write.writable_roots=[] plus the same --add-dir appended ROOM.md entry 22bd1d92, proving the absolute root works only with an override; its first turn also lacked the digest. I did not observe the required resumed/main-checkout sessions, protected-path denials, or /hooks trust. The second run used a config override, not a hook-trust bypass."

[[criteria]]
id = "c7"
verdict = "pass"
evidence = "I read work/BOARD.md, the shared-memory skill and ADR 0018. The board page names what it decides and preserves work-check, work-accept, CI and Tom; both pages describe the room as short-term, board.py memory add/search/index, record schema, caps and never-store list. ADR 0018 names DECISION #193 section 4 in its superseded-by line and retains the original server choice and Basic Memory runner-up. This CLI order has no visual screenshot artifact."

[[findings]]
severity = "high"
file = ".codex/config.toml"
text = "In a linked worktree, ../.git/board resolves below the .git file. Codex 0.156.1 Seatbelt refuses all tool launches even when --add-dir supplies the absolute common board directory. The documented linked-worktree launch cannot perform c6; an explicit one-run override to an empty writable_roots list permits the append."

[[findings]]
severity = "high"
file = ".codex/hooks.json"
text = "Neither fresh installed-client session showed the SessionStart board digest before its first tool call. The second session could run commands and append to the board, so this is separate from the Seatbelt failure. c6 requires the digest and hook trust through /hooks; running the tracked command under sh does not prove client behavior."

[[findings]]
severity = "medium"
file = "work/orders/board-and-memory/order.toml"
text = "The c5 validator currently fails because board-and-memory and the concurrently checked-out harness-core both own .codex/config.toml. The stack is expected to address this, but the acceptance command is red in the present workspace; rerun after the ownership sequencing change is available."

[[findings]]
severity = "low"
file = "work/BOARD.md"
text = "The live memory digest still presents the superseded MCP design as current: decision:shared-memory names the official memory server, and gotcha:memory-substring-search advises search_nodes. Replace those facts at landing so the first-turn digest teaches the shipped file and index design."
