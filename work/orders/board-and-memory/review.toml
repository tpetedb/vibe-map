v = 1
order = "board-and-memory"
by = "reviewer-board-and-memory-r1"
reviewed = "ea2d9aa073a94bb24dba277e3487a1a2bd234f69"
verdict = "accept"

# Delta review since 452a97f: bc7ec58 (codex_hooks_state with Codex's trust
# hash, the reason line, the launcher tests) and ea2d9aa (c6 restated by the
# manager). c6 is left to the Codex reviewer, as the orchestrator asked; it is
# marked fail below only because nobody has ruled it yet, not because of a
# defect. Both findings of the last round are dealt with, see c1, c6 and c7.

[[criteria]]
id = "c1"
verdict = "pass"
evidence = "I ran uv run pytest on tests/test_board.py, tests/test_memory.py and tests/test_work.py on this head: exit 0. In git-archive scratch copies of ea2d9aa I reverted the launcher wiring three ways: root = top in launch_codex, codex_hooks_state called without the main checkout root, and the reason line formatted with the worktree path. Each fails test_the_launcher_in_a_linked_worktree_asks_the_main_checkout and test_the_launcher_says_the_main_checkout_must_carry_the_hook, and the unmodified copy passes, so the low finding of the last round about the untested launcher is fixed. The c1 behaviours the delta did not touch (lock, ids, digest size, lint, two CLI writers, index) were ruled pass on f2fe6ee and their tests still pass."

[[criteria]]
id = "c2"
verdict = "pass"
evidence = "I ran the c2 command on this head: exit 0, memory-lint reports 11 entities. git grep finds no codex-trust outside a test that asserts its absence. The delta touches none of the server removal."

[[criteria]]
id = "c3"
verdict = "pass"
evidence = "I ran the c3 command: exit 0. The reader in this worktree and in the main checkout /Users/maxgroupit/vibe-map resolves the same room, and both outputs are the same 8,750 characters. The delta does not touch read()."

[[criteria]]
id = "c4"
verdict = "pass"
evidence = "I ran the c4 command: missing [], .codex/config.toml has no writable_roots, and --add-dir is in the justfile and in BOARD.md. With a fake codex on PATH, board.py codex hello from a scratch linked worktree passed --add-dir with <main>/.git/board, the absolute common dir."

[[criteria]]
id = "c5"
verdict = "pass"
evidence = "On this head I ran the whole c5 command: ruff check, ruff format --check, checks.py style and sinks, the six generator --check runs, work.py validate (39 orders, 0 collisions this checkout answers for) and the template grep all exit 0, and the non-browser battery (pytest -m 'not browser and not integration') exits 0."

[[criteria]]
id = "c6"
verdict = "fail"
evidence = "Not ruled by me; the Codex reviewer proves this and has not yet. What I checked from this side with codex-cli 0.156.1: I built a scratch repo with a linked worktree, put this branch's .codex/hooks.json in the main checkout and used a temporary CODEX_HOME. app-server hooks/list for the worktree then lists sessionStart with key <main>/.codex/hooks.json:session_start:0:0 and trustStatus untrusted until an approval is recorded. With a fake codex, board.py codex fed the digest and named the reason in each state that was off. The live parts of the text, not run by me, are the fresh and resumed sessions appending to ROOM.md, the denied writes to .agents/ and .git, and no MCP server on either client. The last round's medium finding on this text is dealt with: the restated c6 no longer names just codex-trust or an un-enrolled worktree."

[[criteria]]
id = "c7"
verdict = "pass"
evidence = "I read the changed bullet in work/BOARD.md and the troubleshooting row in the shared-memory skill against the code. Both say the hook counts as on only when the project is trusted, the main checkout's .codex/hooks.json carries it, and /hooks has it enabled with the current hash. Both say an edited, turned-off or missing hook still gets the digest, and that just codex names the reason in one line, which is what I saw it print. What the board decides and never overrides (work-check, work-accept, CI, Tom), the schema, caps, never-store list, memory add|search|index and ROOM.md as short-term are unchanged. ADR 0018 keeps Superseded by: DECISION #193 section 4 and the Basic Memory runner-up row. The seeded gotcha:memory-substring-search now reads 'memory search keeps a row only if every word is a substring ... src: board.py search', which matches search() in board.py; search_nodes appears only in the ADR's historical consequences."

[[findings]]
severity = "low"
file = "work/BOARD.md"
text = "The digest still teaches the retired server through another seeded entity. Live memory.jsonl holds decision:shared-memory 'Official MCP memory server pinned 2026.8.31, writes serialized, read_graph off; switch to Basic Memory at about 300 entities, src: ADR 0018'. board.py read prints it on line 17 of every first-turn digest, under 10 newest decisions. That contradicts DECISION #193 section 4 and the ADR's superseded-by line. It is runtime data, not part of this tree, so it does not block landing. At landing, one board.py memory add decision:shared-memory ... --replace naming files plus index.json and no server fixes it. The gotcha fixed in this round was the other one of these two."

[[findings]]
severity = "low"
file = "tools/board.py"
text = "Under a python3 without tomllib (the macOS /usr/bin/python3 3.9.6 that the hooks are written for), codex_hooks_state always returns unknown. just codex then prints 'the digest goes in the first prompt, as Codex's user config could not be read', even when the config is fine and the hook is approved, and the digest arrives twice. I reproduced this by running /usr/bin/python3 tools/board.py codex with a trusted and approved scratch config. On this machine just codex gets Python 3.12 from PATH and says the right thing. The unknown reason should say the Python lacks tomllib, or the recipe should run under uv, as memory-add does."
