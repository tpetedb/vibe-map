# The harness team's manager agreeing to Galaxy's tests, generated checks and
# boot-order dependency. It covers the implementation commit reviewed below.
v = 1
order = "galaxy-v0"
team = "harness"
by = "manager-harness"
reviewed = "f9d9a6b80f8330b02143dcb354da0fcee447f31e"
verdict = "accept"
note = '''
Accepted for the harness team at f9d9a6b80f8330b02143dcb354da0fcee447f31e.

The Galaxy browser test now uses the 1440 by 900 desktop fixture and runs the
same phone assertions on Chromium with Pixel 7 metrics and WebKit with iPhone
15 metrics. The declared Galaxy and experience suite passed all 20 tests. Its
four island captures matched their goldens at mean 0.0000 and worst 0.000, and
both deliberate mutations were detected.

Ruff check and format are clean for the changed Galaxy test. All six generated
checks pass. The size check uses commit 541df67 as the pre-Galaxy baseline and
reports a 36,689 byte increase, within the order's 80 KB limit.

The final delta adds three place-specific dome captures to the desktop browser
test. That focused test and the shared Android and iPhone test pass. The built
game and packaged fork source match the corrected Chart camera and place
landing source byte for byte. The final isolated increase is 36,689 bytes.

Boot remains last by construction: tools/build.py gathers the game and Galaxy
module folders, removes game/90-boot.js from that sequence, then appends it.
The focused build, module and Galaxy selection passed 12 tests, including the
optional second folder, late game modules, missing boot and invalid module
names.

The final metadata-only change runs the style and sink checks through
`uv run python`, where the project's Rich dependency is available. Both
corrected commands pass.
'''
