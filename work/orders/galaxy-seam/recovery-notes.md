# Recovery

The original seam and golden evidence remain in branch history. Recovery makes
compound travel use the shared `walkTo()` path, labels the finale accurately,
replaces queued destinations during flight, and cancels navigation state when
the experience is disposed. Each behavior has a browser regression.
