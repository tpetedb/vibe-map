v = 1
order = "galaxy-seam"
by = "codex-galaxy-seam-review-r2"
reviewed = "b45ec624ecc6759d22e0c136dc51db62aaa461be"
verdict = "accept"

[[criteria]]
id = "c1"
verdict = "pass"
evidence = '''
Ran `uv run pytest -q tests/test_game_experience.py` on the reviewed checkpoint with Chromium permitted to launch: all 21 cases passed. This includes four live golden comparisons, each at mean 0.0000 and worst 0.000, as well as contract behavior, cross-island compound travel, replacement of an in-flight destination, finale identity, disposal, and the comparison arithmetic itself. I also opened and inspected the campus, winter, desert and prod captures; each is a complete fitted island view with its expected terrain, structures and bridges, and no overlay or text contamination.
'''

[[criteria]]
id = "c2"
verdict = "pass"
evidence = '''
Ran every component of the criterion with the project venv first on PATH and a writable uv cache. Ruff check and format check were silent; the style check reported no em dashes or emoji; the sink check exited zero; and the complete `pytest -q -m 'not browser and not integration'` battery reached 100% with exit zero. The first sandboxed attempt could not bind the suite's loopback servers or reach its Docker fixture, so I reran the same battery outside that sandbox; those were environment errors, not product failures.
'''

[[criteria]]
id = "c3"
verdict = "pass"
evidence = '''
Ran the order's six `--check` generators in sequence. Build reported that game/vibe-map.html matches src; tree, cookbook and syllabus reported OK; sync_template and sync_fork_source exited zero. A direct diff between src/game/24-experience.js and its fork_source mirror is empty.
'''

[[criteria]]
id = "c4"
verdict = "pass"
evidence = '''
Ran the unchanged four-image comparison three times. All twelve comparisons passed at mean 0.0000 and worst 0.000. The full suite independently rewrites the served built game before page load: campus grass changes from #79CC72 to #79CC7D in exactly one source occurrence and produces mean 0.8194, worst 2.949; removing the campus well in exactly one occurrence produces mean 0.0276, worst 1.008. Both fail the tightened 0.002 mean / 0.1 worst guard. I opened both changed captures as well as all four unchanged captures and confirmed the mutations correspond to the rendered scene. The test's measurement note explains the fixed seed, frame clock, at-rest condition, font removal and 16x10 floating-point regional means; it records Mac and Linux software-renderer noise and explicitly lists smaller prop changes below the guard's resolution. This directly addresses the stale review's tolerance finding.
'''

[[criteria]]
id = "c5"
verdict = "pass"
evidence = '''
Read the contribution and the recovery diff. The order adds src/game/24-experience.js rather than editing a pre-existing src/game module; its fork_source copy is identical, and the built output is generated and in sync. The surrounding train edits existing modules for other reviewed orders, which is why aggregate main...HEAD output contains strays, but they are not this order's contribution. Browser tests call every contract member through `window.__experience()`: name and registry shape, build, dispose, tick, goTo, where and listing. The recovery tests additionally prove compound travel lands and walks through the shared `window.walkTo()` seam, a newer destination replaces an in-flight one, the finale is not exposed as stop 9, and disposal cancels flight, aim and proximity. The four inspected golden captures match their records exactly, providing direct evidence that the island rendering stayed fixed.
'''
