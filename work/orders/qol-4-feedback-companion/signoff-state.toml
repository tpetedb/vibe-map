# The state team's agreement on the order's change to src/game/00-state.js.
# Renewed after the recovery changed another owned source file, tests and order
# metadata; the state-owned implementation itself is unchanged.
v = 1
order = "qol-4-feedback-companion"
team = "state"
by = "manager-state"
reviewed = "bf0d1a2ce38060d479aec731a2db4f56928acc85"
verdict = "accept"
note = '''
Accepted at the current implementation and review head. The recovery contribution
does not change src/game/00-state.js or any other state-owned path, but it changed
the order contribution and therefore required this sign-off to be renewed.

The accepted state behavior remains the same:
- save(mark) raises the saved indicator only after localStorage accepted the
  record, and save(false) lets message-log writes avoid raising it.
- S.hints and S.notes have defaults and are validated entry by entry during load.
  They remain local browser state and do not enter the versioned progress code.
- visibility handling pauses and restores the shared game clock when battery
  saver is active, so hidden wall-clock time does not advance island time.
- src/game/00-state.js is byte-identical to its fork-source mirror, and the built
  game matches src/ at this head.

Non-browser validation at bf0d1a2ce38060d479aec731a2db4f56928acc85:
- just work-validate: 29 orders read and zero active ownership collisions; it
  separately reported an unrelated draft order whose owns list is empty.
- tests/test_game_state.py, test_reset.py, test_config_levels.py,
  test_camp_config.py and test_project.py with browser and integration markers
  excluded: 47 passed.
- tools/build.py --check, tools/sync_fork_source.py --check,
  tools/checks.py style, git diff --check and the source-to-mirror cmp all passed.

The existing low findings remain follow-up work: NOTE_CAP belongs with the saved
record constants; note timestamps and text plus the hints list need bounds;
track() should not raise a progress-saved mark for its minute tick, or the comment
must describe that behavior; and the battery-saver-off comment should say only
the clock keeps running while browsers suspend hidden frames. These do not make
the state change unsafe and did not block the accepted review.
'''
