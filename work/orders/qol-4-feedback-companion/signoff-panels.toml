# The panels team's manager on what this order does to the panels files. One
# of ours is changed, src/game/90-boot.js, by three lines; src/body.html,
# src/head.html, src/style.css, src/errors.js, src/icon.svg and every other
# panels-owned module are untouched by this branch. Read at the current review
# head below, including the recovery change to the scene-owned avatar loop.
v = 1
order = "qol-4-feedback-companion"
team = "panels"
by = "manager-panels"
reviewed = "bf0d1a2ce38060d479aec731a2db4f56928acc85"
verdict = "accept"
note = '''
Refresh at bf0d1a2: accepted again after the recovery review. The panels-owned
delta remains only the visibility listener in src/game/90-boot.js. The recovery
changes the achievement cadence in scene-owned src/game/18-avatar.js from a
capped movement delta to the same elapsed clock that onVisibility pauses. A
sparse visible frame may therefore raise an earned achievement and its recorded
toast on schedule; hidden time still raises nothing, and the panels interaction
contract accepted below is unchanged.

Accepted: the change to src/game/90-boot.js is true of the code, is the listener proposal 7 asks for in that file by name, and is written the way the file is. Nothing I want changed holds the landing. What follows from it for the panels team is real and is mostly work in our own files, through our own orders, named below.

What I read and ran, as the owner of the files:
- `git diff origin/main...HEAD` on the team's paths: lines 49 to 51 of src/game/90-boot.js and nothing else. The comment is two lines, says why, names the function and the file it lives in and no line number. The listener is wired after the boot chain and before `init3d()`, which is safe because `pageHidden()` and `pageVisible()` ask `hasClock()` first, and a page that loads in a background tab gets no event and no frame, so there is nothing to pause. `onVisibility` has this one caller (grep), so without the line `__awake().hidden` can never turn true, which is what the second review's mutation saw.
- Through the real listener in the built game, Chromium at 1440x900 and 412x915, zero page errors: hidden gives `__awake()` hidden true with the clock held, and the return starts it where it stopped (17.6230 s hidden, 17.6235 s one frame after coming back). Battery saver off I did not drive by hand; the order's own test for it is one of the six below.
- The team's own tests on this commit, `-m "not integration"`: tests/test_game_qol.py 18, test_game_qol_settings.py 16, test_game_smoke.py 24, all 58 green, tree clean afterwards. Of the order's tests, the hidden page, the saver off, the mark and the hint: 6 green. `cmp` says the fork mirror of 90-boot.js is the same, `tools/build.py --check` and `tools/sync_fork_source.py --check` exit 0, `tools/checks.py style` OK, `just work-validate` 25 orders and 0 collisions.
- What else of ours depends on what changed. `save()` has a parameter now and no panel hands it one (40-sheet.js 5 calls, 85-settings.js 4, 87-onboarding.js 4, 70-minigames.js 3, 88-chat.js 3, 71-finale.js 2, 86-interests.js 1, all 22 bare; the one callback is `setTimeout(save,250)` in 23-camera.js, which passes nothing), so every one of them now raises the Saved mark, throttled. `toast()` keeps its signature and `__toasts()` still counts under Quiet, which our settings tests wait on. No panel module calls `toast()` itself. `showHintsAgain()` in 85-settings.js line 82 already wrote `S.hints`; `hint()` reads the same list through `sl()`, so the button and the helper agree. The play tick in 89-dashboard.js line 260 already asks `document.hidden`, so the Stats half of proposal 7 needed nothing from us. src/game/40-sheet.js line 64 has a `visibilitychange` listener of its own for the wake lock; the two answer for different things, neither depends on the order they run in, and "One listener" in 00-state.js is true of the pause.

Looked at, on our surfaces (pictures under /tmp, nothing written to the tree):
- The title on a phone while a name is typed: `nameTyped()` saves on every key, so the mark is up for most of the typing, box [336,14,398,40], over WORKSTREAMS, the last word of the eyebrow line. On the desktop it sits in clear sky. With Settings open on a phone it prints over the More button, and the Tip card covers the Backpack's heading for five seconds. Taps pass through in every case. All of it is where `#toast` sits (src/style.css line 505), which is ours and is B2.
- Under Quiet the mark's parent computes to display none on both profiles while `__saved().on` is true: src/style.css line 629, `body.quiet #toast{display:none}`, hides the whole stack. Since `toast()` now returns before it raises a card under Quiet, what that rule still hides is the mark and a card that was already up when Quiet was chosen. Our own test pins it without meaning to: tests/test_game_qol_settings.py line 371 asserts `#toast` is not visible after a claim under Quiet, and a claim now puts the mark in there.
- My ruling on the second review's second finding, as the owner of the setting: Quiet means the mark too. The player who chose it asked for nothing to pop up during a lesson, `pitchTyped()` and `nameTyped()` save on every key, and the mark is reassurance, not information the log has to keep. So the behaviour on this branch is the right one; what is missing is that it is meant, and said.

What I would like in the follow-up for the second review's lows, all low, none of it in our files:
1. src/game/18-avatar.js line 207, `noteWhen()`, spells the clock a second time: `fmtDay()` and `fmtClock()` are in src/game/89-dashboard.js lines 33 and 34, in the same closure. The review's finding about yesterday's 22:26 is answered by using the two of them, not by a third formatter.
2. src/game/18-avatar.js, `savedTick()`: under Quiet it returns before it builds anything, the way `toast()` does, so that the mark's silence is a line of code with a comment and not a side effect of our stylesheet. The changelog's last entry gets the same condition.
3. src/game/18-avatar.js line 192 asks whether the Backpack is open by the screen's `on` class. `closeSheet()` takes `on` off `#sheet` and leaves it on the screen (measured: after `openPack('log')` and `closeSheet()`, `#sheet` is off and `#s-pack` is still on), so after one look at the log every pickup rebuilds a closed panel until another sheet is opened. Harmless, and our own `applySettings()` does the same; see the helper below.
4. src/game/90-boot.js line 50, only if the file is edited again for another reason: "comes back on the frame it left" is true with Battery saver on auto, and one clause would say so. Such an edit ends this sign-off as it ends the review, and I read again.

What the panels team does after this lands:
- hunt-b-panel-chrome (ours, owns src/style.css, not built yet). B3 will no longer reproduce as filed: the cap of three cards is in `toast()` now, so its builder rejects the script half with that evidence and keeps the stylesheet half. B2 gains three lines in its findings before it is built: the stack holds the Saved mark as well as the cards, so the new place has to clear the HUD, an open sheet's Close and heading, and the title's eyebrow at 412 wide; line 629 keeps hiding the whole stack under Quiet wherever the stack goes, per the ruling above; and `.saved` gets a rule next to `.tst`. The twelve inline declarations at 18-avatar.js line 226 win over a stylesheet until they are taken out, so the follow-up that owns 18-avatar.js names hunt-b-panel-chrome in `needs` and drops them after it.
- hunt-w-settings-news (ours, owns src/game/85-settings.js, 40-sheet.js and 18-avatar.js, not built yet). It shares 18-avatar.js with this order and with the follow-up, so it is never in a launch group with either, and 18-avatar.js is 86 lines longer, so the line numbers in its findings have moved. It takes the builder's note about "Battery saver: Keep running" (85-settings.js line 23): the row stays, because the idle half rate that qol-2-wayfinding builds in 31-animate.js gives off something real to keep running, and the two labels are rewritten once that has landed so that each says what it does for the hidden page and for the idle one. The Toasts row (line 25, "Quiet: none shown") says that Quiet covers the Saved mark as well. It can also give 40-sheet.js one helper that says whether a given screen is open, from `sheetOpen`, for `applySettings()` and for `toast()` line 192.
- A small new panels order for proposal 22's call sites in our files: `hint()` from `openVault()` and `openTree()` in src/game/60-vault.js, which no active order owns. The ids are fixed strings and are written down in the order, because a renamed id shows an old hint again. It waits for hunt-b, so that the card it raises does not sit on the vault's own header.
- qol-5-continuity-photo owns src/game/89-dashboard.js with panels in `cross`. When it brings the notifications list to Stats, the list reads `sl("notes")` and formats with `fmtDay()` and `fmtClock()`; `window.__log()` is a test seam by its name and stays one. I say so on that order when it asks for our sign-off.
- The Backpack has a fourth tab, so the caption of `backpack.png` in docs/media/README.md is one tab short. The second review already gave that to the docs team; nothing for us to do but not forget it at the next `just media`.
'''
