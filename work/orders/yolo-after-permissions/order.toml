v = 1
id = "yolo-after-permissions"
title = "just start offers Claude's YOLO mode only after the permissions lessons, behind a plain warning, and the first lesson points at the lessons that teach permissions"
team = "cli"
branch = "claude/yolo-gate"
goal = "ways-of-working"
issue = 201
# Whoever builds this writes their name here; a review by the same name does not count.
builder = ""

owns = [
  "vibemap/tui.py",
  "tests/test_tui.py",
  "src/body.html",
  "tests/test_safe_copy.py",
  "changelog.d/yolo-after-permissions.fixed.md",
]
cross = ["panels", "docs"]
needs = []

[[criteria]]
id = "c1"
text = "The TUI tests pass, including a new one that fails on main: with the permissions lessons not done, the start menu offers no YOLO entry (or offers it only behind a warning naming what it skips), and with them done it does."
check = "uv run pytest -q tests/test_tui.py tests/test_onboarding.py tests/test_safe_copy.py"

[[criteria]]
id = "c2"
text = "Lint, style, sinks, the non-browser battery and every generated file in sync; every order still reads."
check = "uv run ruff check -q . && uv run ruff format --check -q . && python3 tools/checks.py style && python3 tools/checks.py sinks && uv run pytest -q -m 'not browser and not integration' && for c in build regen_tree gen_cookbook sync_template sync_fork_source gen_syllabus; do uv run python tools/$c.py --check || exit 1; done && uv run python tools/work.py validate"

[[criteria]]
id = "c3"
text = "The gate reads progress from state (one helper, not a check spelled at the call site), the warning is plain and true (it names that every permission prompt is skipped), the first lesson's pointer names the lessons that teach permissions (Evening 3 stop 4 and Evening 4 stop 5, by their titles), and no em dashes."
judge = "reviewer"
