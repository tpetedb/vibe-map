# Another team's manager agreeing to a change in that team's files. Copy to
# work/orders/<id>/signoff-<team>.toml. Same rule as a review: it covers one
# commit and lapses when the owned files move.
v = 1
order = "yolo-after-permissions"
team = "panels"
by = "manager-panels"
reviewed = "4bdb2b5506cdc7024970a36c7203ad279a7f6821"
verdict = "accept"
# What the other team has to know, or to do after this lands.
note = """
Checked git diff origin/main...HEAD restricted to panels paths: one line in
src/body.html (the Definition of done callout in the first lesson). It drops
the false "workstream 4" pointer and names the two stops that actually teach
permissions, Hooks as gates (Evening 3, stop 4) and Claude Code, the power
settings (Evening 4, stop 5). Opened vibemap/data/campaign.json myself: those
are the two titles, and the TUI's own hint builds from the same source, so
the wording will not drift out from under it. Matches house style: plain,
true, no em dash. No panels-owned test (test_game_qol.py,
test_game_qol_settings.py, test_game_smoke.py) references the old string, and
nothing else of panels' depends on this line. Nothing for panels to do after
this lands; the one open follow-up (docs/SYLLABUS.md still carries the old
sentence) is correctly left to the open #190/#203 orders, not this one.
"""
