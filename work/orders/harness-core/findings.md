# harness-core

The task in plain words: build the thing a person edits (`config.toml` at the repo root), the three complete profiles, the model table with Tom's preferences, the `.agents/` folders that hold configuration and templates, and the generator `harness.py` with `sync`, `check`, `doctor`, `explain` and `pick`. Render `.claude/settings.json`, `.codex/config.toml`, `.codex/hooks.json`, `CLAUDE.md`, `.claude/agents/` and `.codex/agents/` from that source, and commit them. Teach `tools/work.py` that a path listed in `.agents/generated.lock` is regenerable (like the GENERATED list), so the orders that follow can regenerate outputs without owning them.

Implements: DECISION-final.md section 1 (all three TOML blocks, amendment A1), section 2 (the derivation table, the generator, the runtime store rule with the single Codex board root, amendments A2 and A3), section 3's floor validation of overlays (A3), and the doctor's two-part trust report (A4).

Notes for the builder: `hooks.toml` and `policy.toml` are created here with today's hooks (the two SessionStart readers, the data backup, the work-order guard) and the deny floor, so that the rendered settings lose nothing; harness-hooks takes them over afterwards. `roles/` holds today's three roles (builder, reviewer, team-manager) so the agents render; harness-supervisor adds chief, manager and scout. `jobs/` and `pipelines/` are not created here. Templates name their output in their first line, so a later order adds a template without touching `harness.py`. `tools/sync_main.py` belongs to the active Codex order harness-sync-main-fail-fast and is not touched; the GENERATED list gains the rendered outputs in harness-portable. `tools/work.py` was owned by harness-work-ci-followups, landed in #188; prune that worktree if `work-validate` still counts it.
