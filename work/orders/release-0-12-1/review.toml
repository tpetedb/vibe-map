# Round 2. Read the order, findings.md, the round 1 review, and the whole diff
# (git diff origin/main...HEAD, origin/main still at 7c13086, car M #214
# merged, HEAD 435b700). Reran c1 to c4 myself outside the work-check cache,
# mutated the changelog and the Galaxy source in a scratch copy
# (git archive of HEAD under /tmp) to see the two new guards bite, and checked
# each claim of the section against src/ and vibemap/.
v = 1
order = "release-0-12-1"
by = "reviewer-release-0-12-1-r2"
reviewed = "435b7002941b42c8fc51e1199635b015a6366ef5"
verdict = "accept"

[[criteria]]
id = "c1"
verdict = "pass"
evidence = """
Reran `uv run pytest -q tests/test_changelog.py tests/test_build.py tests/test_repo.py` on 435b700: all green. Those three files are the changelog, build and repository guards, so the command is the sentence. tests/test_changelog.py now carries two 0.12.1 guards (the round 1 findings, turned into tests). Both bite, in the scratch copy: putting back the round 1 wording ("Vault set to Grow and no topic done, ... opens the next lesson,") fails test_the_0_12_1_grow_line_says_what_galaxy_opens; dropping "In the game, " from the YOLO line fails test_the_0_12_1_permissions_pointer_is_scoped_to_where_it_is_true; changing readable() in src/galaxy/20-experience.js to filter only "next" fails the Grow guard again, so the line is tied to the source it describes.
"""

[[criteria]]
id = "c2"
verdict = "pass"
evidence = """
Reran: prints `0.12.1 []`, exit 0. Round 1 showed it bites (pyproject back to 0.12.0, or a fragment restored, exits 1); the checked text has not changed since. The sentence's wider reach checked by hand again on 435b700: uv.lock has vibe-map 0.12.1 and `uv lock --check` resolves clean, `uv run vibe --version` prints `vibe, version 0.12.1`, the built game carries `const CONFIG={"version": "0.12.1"`. changelog.d holds only README.md; the two deleted fragments are exactly car M's two. Links: Unreleased compares v0.12.1...HEAD, [0.12.1] compares v0.12.0...v0.12.1.
"""

[[criteria]]
id = "c3"
verdict = "pass"
evidence = """
Reran the whole chain on 435b700: ruff check and format clean, `style OK: no em dashes, no emoji`, sinks quiet, the non-browser battery all dots, exit 0. The command is the sentence. CI on PR #216 at 435b700 agrees: in the fast job every step passed except the last, `work orders are readable, reviewed and inside their files`, which failed only because the round 1 review had lapsed; this review is what that step waits for.
"""

[[criteria]]
id = "c4"
verdict = "pass"
evidence = """
Reran the six `--check` tools: all exit 0. The only generated change on the branch is the CONFIG version string in game/vibe-map.html, which `tools/build.py --check` ties to the package version. No src/ file changed on the branch, so nothing else could drift. The command is the sentence.
"""

[[criteria]]
id = "c5"
verdict = "pass"
evidence = """
Read the section as it stands. Short: one heading, `### Fixed`, two bullets, one per car M fix (#211 for #209, #213 for #201). No em dash or en dash in it (scripted scan), plain words. Released sections untouched: from `## [0.12.0]` to the link block the file is byte-identical to origin/main (scripted compare); the only other changed line is the old Unreleased compare link.

Grow line, true of main: galaxyModel (src/galaxy/00-model.js) marks exactly one topic "next", the first not done, and every completed one "done"; readable() in src/galaxy/20-experience.js returns every topic whose state is not "ahead". So "the next lesson on the journey, and every lesson already done" is what opens, "topics further along stay earned" is the ahead filter, and readable() lives in the Galaxy experience only, so the islands are as before. "Settings, Vault set to Grow" matches the Settings label "Grow: notes unlock as you play" (src/game/85-settings.js). Round 1's medium is fixed with the suggested wording.

YOLO line, true of main: `vibemap.tui.yolo_locked_hint()` prints "skips every permission prompt; opens once you have done Hooks as gates (Evening 3, stop 4) and Claude Code, the power settings (Evening 4, stop 5)", knows_permissions() needs both PERMISSION_STOPS, the launcher title is "Claude, YOLO mode" with `claude --dangerously-skip-permissions`, the toolbelt button "YOLO: install everything" is unchanged, and src/body.html's first Definition of done points at the two lessons. The pointer is now scoped "In the game," because docs/SYLLABUS.md line 165 still says "workstream 4"; round 1's first low is fixed.
"""

[[findings]]
severity = "low"
file = "tests/test_changelog.py"
text = """
test_the_0_12_1_grow_line_says_what_galaxy_opens pins the literal source text `filter(t=>t.state!=="ahead")` in src/galaxy/20-experience.js. A harmless reformat of that line (a space, a renamed parameter) fails a changelog test on a branch that never touched the changelog. It does the job today and bit in the scratch copy; a follow-up could read the rule through a behaviour (the galaxy state helper) rather than the spelling. Land as is.
"""

[[findings]]
severity = "low"
file = "work/orders/release-0-12-1/order.toml"
text = """
Carried from round 1, still true and still no change needed: c2 reads only pyproject.toml while its sentence (and the goal's "everywhere a tool reads it") also covers uv.lock and the built game's CONFIG. Both are right (checked by hand, see c2) and c4 catches the game. A future release order could add `uv lock --check` and `uv run vibe --version` to c2.
"""

[[findings]]
severity = "low"
file = "work/orders/release-0-12-1/order.toml"
text = """
Not a defect of the work, a landing step: the order has cross = ["docs"] (CHANGELOG.md matches the docs team's "*.md"), and there is no signoff-docs.toml yet. `just work-accept` will want one bound to 435b700 from the docs manager, as release-0-12-0 had. The syllabus line that still says "workstream 4" stays with the open #190/#203 orders; once it changes, the conditional permissions guard goes quiet by design.
"""
