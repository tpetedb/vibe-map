# release-0-12-1

Build only on main after car M (#214) has landed: `just sync-main`, then `just release 0.12.1`. The section is short: Fixed, the two fragments of car M (galaxy-grow-first-lesson, yolo-after-permissions) in plain words. Remove the Grow-mode caveat from the 0.12.0 Galaxy line? No: released sections stay untouched. Rebuild, run every criterion, commit by explicit path, push, open the PR with the tag line first.

builder:
