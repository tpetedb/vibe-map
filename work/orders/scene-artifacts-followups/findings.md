# Source

The lows the accepted review of hunt-f-artifacts left (`git show origin/main:work/orders/hunt-f-artifacts/review.toml`) and the hunt-b-panel-chrome findings its review handed on to the artifacts, sheet and vault files (`gh issue view 110 --comments`, PR #157 and the order folder on the prereq train #185). Examples named by the board: ring slopes, the vault link focus ring, comment discipline, the stall sentence in campaign.json.

# Also

The three photograph tests in tests/test_game_hunt_f.py cost 354, 316 and 92 seconds on CI, most of the smoke shard. Make them wait on facts the page produces (a rendered frame, a state) instead of long settles, without weakening what they prove.

Do not touch src/body.html, src/style.css, src/game/85-settings.js or src/galaxy/ (frozen for #183).
