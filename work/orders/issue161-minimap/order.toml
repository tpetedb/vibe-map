v = 1
id = "issue161-minimap"
title = "Keep the minimap clear of zoom controls in short windows"
team = "scene"
branch = "codex/issue161-minimap"
goal = "release-0-11-0"
issue = 161
builder = "codex-issue161-minimap"

owns = [
  "src/game/32-minimap.js",
  "tests/test_minimap_short_window.py",
]
cross = []
needs = []

[[criteria]]
id = "c1"
text = "At 640x480, 720x600 and 800x640 the minimap canvas and Bigger button do not intersect the zoom column; the 800x640 minimap keeps its original corner."
check = "uv run pytest -q tests/test_minimap_short_window.py::test_short_window_keeps_zoom_clear_of_minimap"

[[criteria]]
id = "c2"
text = "The built game and installed fork source match their source."
check = "uv run python tools/build.py --check && uv run python tools/sync_fork_source.py --check"

[[criteria]]
id = "c3"
text = "The fix keeps the rendered map and Bigger button usable and clear of zoom in the short window."
judge = "reviewer"
