v = 1
id = "galaxy-echo-jump"
title = "Jump from a Galaxy echo to its primary site"
team = "scene"
branch = "codex/galaxy-echo"
goal = "galaxy"
issue = 168
builder = "codex-sol-galaxy-echo"
owns = ["src/galaxy/20-experience.js", "src/galaxy/60-flight.js", "src/style.css", "tests/test_game_galaxy_echo.py"]
cross = ["panels"]
needs = ["galaxy-flight"]

[[criteria]]
id = "c1"
text = "A showcased secondary origin is named an echo and gives keyboard, pointer and list-only players a Jump to primary action. Only the current action group is tabbable. Jump uses the existing flight/cut and lands with the chosen topic selected and focused at its actual primary place without altering shared progress; Learn at the echo remains available."
check = "uv run pytest -q tests/test_game_galaxy_echo.py"

[[criteria]]
id = "c2"
text = "Existing Galaxy journey, chart, walking and flight flows still work."
check = "uv run pytest -q tests/test_game_galaxy.py tests/test_game_galaxy_flight.py"

[[criteria]]
id = "c3"
text = "The built game and installed fork source match the edited source."
check = "uv run python tools/build.py --check && uv run python tools/sync_fork_source.py --check"

[[criteria]]
id = "c4"
text = "The echo marker tells the historical truth without overwhelming the lesson, and the two actions remain legible and reachable at phone size and enlarged text."
judge = "reviewer"
