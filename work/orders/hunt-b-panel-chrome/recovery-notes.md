# Recovery

The original implementation and review remain in branch history. The recovery
builder changed the two tablet cases to use the repository's deterministic
FAST rendering profile after CI timed out before their layout assertions. The
touch pointer, viewport, DPR, overlap and page-error assertions remain active.
