# .agents/tasks/

A pointer, not a ledger. A task for an agent is a work order under
`work/orders/<id>/order.toml`, checked by `tools/work.py` (`work/README.md`,
the skill `work-order`). Nothing is kept here.
