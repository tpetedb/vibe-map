- `just start` no longer offers a beginner "Claude, YOLO mode": the button that
  starts `claude --dangerously-skip-permissions` stays greyed out, with a hint
  saying it skips every permission prompt, until state records both lessons
  that teach permissions, Hooks as gates (Evening 3, stop 4) and Claude Code,
  the power settings (Evening 4, stop 5). The toolbelt's "YOLO: install
  everything" button is unchanged.
- The first lesson's Definition of done points at those two lessons for
  permissions in depth, instead of at workstream 4, which does not teach them.
