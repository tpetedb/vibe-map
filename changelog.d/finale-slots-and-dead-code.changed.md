- The finale offers slots ("Friday evening", "Saturday afternoon") instead of
  five 2026 dates and a note to the maintainer, so a camp's configuration and
  the published game never read as out of date. A camp still writes its own
  evenings over them.
- The published site no longer serves the learner's untouched
  `workspace/game/index.html` at `/your-game.html`.
- Removed `interestAnswered()` from the game, which nothing called, and the
  MCP artifact's sample configuration now names the camp's real path instead
  of one from another machine.
