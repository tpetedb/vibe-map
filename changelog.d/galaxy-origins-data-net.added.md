- The four core topics of the data and net shelves say where they happened: `data` and `sql` live at IBM's San Jose lab (Codd's relational model, 1970; SEQUEL, 1974), `http` at CERN (1990) and `apis` at UC Irvine (REST, 2000), with eleven echoes from Berkeley's POSTGRES in 1986 to HTTP/2 at the IETF in 2015. `vibe topic data` and `vibe places ibm-san-jose` show them.
- Each of the fifteen lines stands on a page that was opened and carries the year, the actor and what happened. Left out, and why: the OpenAI API, whose announcement answers a fetch with 403; and Parquet at Twitter and Cloudera, Snowflake, SQLite and GraphQL at Facebook, none of which has a place file yet.
