- A new topic pack, `linux`, on the shell shelf: eleven topics from the kernel and the distributions, the filesystem and permissions, processes and signals, the text tools and scripts that fail loudly, to package managers, tmux, networking from the shell, systemd, cron and timers, and the differences between Linux, WSL and a Mac. `uv run vibe topics linux` lists them.
- Every topic is written from pages opened on 2026-09-23 and carries one primary origin at a place already on the map, from the University of Helsinki in 1991 to a standards body in 2024. `places.problems(packs=["linux"])` is empty.
- Every hands-on runs offline under twenty minutes, and `tests/test_topics_linux.py` builds each one the way its topic says and checks it is red before the work and green after.
