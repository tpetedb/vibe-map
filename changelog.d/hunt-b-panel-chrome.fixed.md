- An open panel starts and scrolls below the HUD, so a lesson scrolled past
  the first screen no longer disappears behind the bar and its pills.
- A toast hangs under the HUD instead of on top of it, so a message about an
  achievement never covers Stats, Vault, Tree, World, More, the map or the
  Close button of the panel underneath. A batch of unlocks leaves a bounded
  stack that shows the newest messages rather than a column down the window.
- The panel is announced as a named modal dialog, so a screen reader says what
  was opened instead of "dialog".
- Opening the More menu on a laptop no longer blanks the numbers beside it.
- A group heading that wraps has room between its two lines.
- A demo terminal on a phone scrolls sideways rather than folding a line in
  half, so its columns line up and a number stays whole.
- A tech tree shelf outside your interests is set back with its surface rather
  than dimmed to a fifth of its contrast, so its description reads on a touch
  screen, where there is no hover to bring it back.
