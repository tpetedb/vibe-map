- An open panel starts and scrolls below the HUD, so a lesson scrolled past
  the first screen no longer disappears behind the bar and its pills.
- A message about an achievement hangs under the HUD instead of on top of it,
  so it never covers Stats, Vault, Tree, World, More, the map or the Close
  button of the panel underneath, and it moves nothing on its way: the map
  keeps its place under the bar rather than stepping down onto the zoom
  buttons. A batch of unlocks leaves a stack bounded by the room between the
  bar and the buttons at the bottom of the screen, in either hand, with the
  newest message at the top and the oldest fading into the bound rather than a
  column down the window.
- A message carries its own surface instead of a blur of what is behind it, so
  it is read over an open panel and over the island alike.
- The panel is announced as a named dialog, so a screen reader says what was
  opened instead of "dialog".
- Opening the More menu on a laptop no longer blanks the numbers beside it.
- A group heading that wraps has room between its two lines.
- A demo terminal folds a line too wide for the panel with a hanging indent:
  the columns keep their left edge, the fold reads as a fold, and the sentence
  a demo ends on is whole on a phone instead of off the right edge.
- A tech tree shelf outside your interests is set back with its surface rather
  than dimmed to a fifth of its contrast, so its description reads on a touch
  screen, where there is no hover to bring it back.
